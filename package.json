{
  "description": "100% functional functions.",
  "devDependencies": {
    "@babel/core": "latest",
    "@babel/polyfill": "latest",
    "@babel/preset-env": "latest",
    "babel-loader": "latest",
    "webpack": "latest",
    "webpack-cli": "latest"
  },
  "files": [
    "dist/"
  ],
  "keywords": [
    "100%",
    "code",
    "functional",
    "functions",
    "methods",
    "library",
    "util",
    "utils"
  ],
  "license": "MIT",
  "main": "dist/fndata.js",
  "name": "fndata",
  "scripts": {
    "build": "npm run test && npm run clean && NODE_ENV=production webpack --mode=production --display=none && echo 'Build Successful!'",
    "clean": "rm -rf dist && mkdir dist",
    "release": "git checkout master && git pull origin master && npm run clean && npm run build && npm publish && echo 'Publish Successful!'",
    "test": "npm run clean && NODE_ENV=development webpack --mode=development --display=errors-only && node dist/test && echo 'Tests Passed!'"
  },
  "version": "0.18.2"
}
