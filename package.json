{
  "name": "fndata",
  "version": "0.0.1",
  "files": [
    "dist"
  ],
  "main": "dist/bundle.js",
  "scripts": {
    "build": "npm run test && NODE_ENV=production webpack -p",
    "release": "git push origin master && git pull origin master && npm publish",
    "test": "node test"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "^7.1.5",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-2": "^6.24.1",
    "webpack": "^4.16.2",
    "webpack-cli": "^3.1.0"
  }
}
