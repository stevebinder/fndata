{
  "description": "Functional methods for manipulating data.",
  "devDependencies": {
    "@babel/core": "^7.1.0",
    "@babel/preset-env": "^7.1.0",
    "babel-loader": "^8.0.2",
    "webpack": "^4.19.1",
    "webpack-cli": "^3.1.0"
  },
  "files": [
    "dist/"
  ],
  "license": "MIT",
  "main": "dist/index.js",
  "name": "fndata",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && NODE_ENV=production webpack -p",
    "release": "git checkout master && git pull origin master && npm publish",
    "test": "NODE_ENV=test webpack --mode=development && node dist/test"
  },
  "version": "0.10.0"
}
