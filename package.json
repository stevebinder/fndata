{
  "description": "100% functional functions.",
  "devDependencies": {
    "@babel/core": "^7.1.0",
    "@babel/polyfill": "^7.0.0",
    "@babel/preset-env": "^7.1.0",
    "babel-loader": "^8.0.2",
    "webpack": "^4.19.1",
    "webpack-cli": "^3.1.0"
  },
  "files": [
    "dist/"
  ],
  "keywords": [
    "100%",
    "code",
    "functional",
    "functions",
    "methods",
    "library",
    "util",
    "utils"
  ],
  "license": "MIT",
  "main": "dist/reactdux.js",
  "name": "fndata",
  "scripts": {
    "build": "npm run clean && NODE_ENV=production webpack -p",
    "clean": "rm -rf dist && mkdir dist",
    "release": "git checkout master && git pull origin master && npm publish",
    "test": "npm run clean && NODE_ENV=development webpack --mode=development && node dist/test"
  },
  "version": "0.11.0"
}
